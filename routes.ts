export const publicRoutes = ["/verification"];

export const authRoutes = ["/signin", "/signup", "/auth-password"];
export const apiAuthPrefix = "/api/auth";

export const DEFAULT_LOGIN_REDIRECT = "/dashboard";
